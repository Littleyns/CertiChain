{
  "name": "certi-chain",
  "version": "1.0.0",
  "description": "Projet de développement Ethereum avec Truffle et Ganache",
  "scripts": {
    "start-ganache": "ganache-cli -s 1 -i 5777",
    "migrate": "truffle migrate --reset",
"delay": "node delay.js",
    "start-dev": "run-p start-ganache delay migrate"
  },
  "dependencies": {
    "truffle": "*",
    "ganache-cli": "*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
